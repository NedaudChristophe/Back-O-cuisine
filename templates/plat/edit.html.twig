{% extends 'base.html.twig' %}

{% block title %}Modification Plat {{ plat.nom }}{% endblock %}

{% block body %}

<div class="container pt-5">

    <div class="row mb-2">
        
        <div class="col-8 mx-auto">
            <h1 class="text-center mb-5">Modification {{ plat.nom }}</h1>
            <a class="btn btn_return mb-5" href="{{ path('plat_index') }}">Retour à la liste</a>
            <div class="col">
                {{ include('plat/_form.html.twig', {'button_label': 'Enregistrer'}) }}
                {{ include('plat/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>

    
{% endblock %}
